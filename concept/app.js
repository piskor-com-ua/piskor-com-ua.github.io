const copy = {
  uk: {
    skip:'До змісту',nav:['Підхід','Напрями','Контакти'],menu:'Меню',close:'Закрити',
    place:'Архітектура та дизайн · Львів, Україна',hero:'Простір.<br>Продуманий<br><span>до відчуттів.</span>',
    heroText:'Від першої лінії до світла, яке зустрічає вас удома.',cta:'Обговорити ваш задум',scroll:'Відчуйте наш підхід',
    concept:'Концептуальна візуалізація',conceptSub:'Образ для демонстрації дизайну сайту',
    introLabel:'Цілісний погляд',intro:'Архітектура, інтер’єр, технології.<br><span>Одна ідея. У кожній деталі.</span>',
    introText:'Поєднуємо простір, матеріали та інженерію в єдине рішення. Від задуму до реалізації — з увагою до того, як ви житимете й працюватимете в цьому просторі.',
    years:'років досвіду',projects:'проєктів',storyLabel:'Один простір · від ідеї до життя',skipStory:'До напрямів ↗',
    sceneNav:['Ідея','Архітектура','Інтер’єр','Реалізація','Автоматизація'],
    scenes:[
      ['Все починається<br>з вашого життя.','Звички, ритм, відчуття. Знаходимо ідею, яка об’єднає майбутній простір.'],
      ['Ідея набуває<br>форми.','Пропорції, природне світло та зв’язок із місцем. Створюємо архітектуру з характером.'],
      ['Краса, що працює<br>щодня.','Інтер’єр та інженерні мережі проєктуємо разом. Комфорт починається з продуманих рішень.'],
      ['Задум стає<br>відчутним.','Камінь, дерево, фактури. Добираємо матеріали й супроводжуємо втілення кожної деталі.'],
      ['Дім відчуває<br>ваш ритм.','Світло, клімат, безпека. Поєднуємо технології у сценарії, природні для вашого життя.']
    ],
    storyNote:'Прокручуйте або оберіть етап',materials:['Камінь','Дерево','Текстиль'],system:'Сценарій «Вдома»',systemRows:[['Освітлення','Тепле світло'],['Клімат','22 °C'],['Штори','Відкрито']],
    serviceLabel:'Наші напрями',servicesTitle:'Від першого задуму.<br>До останньої деталі.',
    services:[
      {title:'Архітектура',sub:'Архітектурні рішення та проєктування',body:'Знаходимо баланс між характером місця, вашим способом життя й можливостями ділянки. Працюємо з житловими, комерційними та виробничими об’єктами.',items:['Концепція, об’єм і планувальні рішення','Архітектурне проєктування та узгодження інженерних рішень','Матеріали фасадів і взаємодія з оточенням','Проєктування об’єктів до класу наслідків CC3 включно']},
      {title:'Інтер’єр та інженерія',sub:'Естетика, ергономіка й невидимий комфорт',body:'Проєктуємо інтер’єр як цілісну систему: від планування й матеріалів до освітлення, клімату та інженерних мереж. Кожне рішення має підтримувати ваш щоденний комфорт.',items:['Планування, стилістика та візуалізація простору','Світло, електрика й інженерні мережі','Матеріали, меблі, механізми та обладнання','Узгодження естетики й технічних рішень']},
      {title:'Реалізація та супровід',sub:'Від добору матеріалів до втілення задуму',body:'Формат участі студії залежить від вашого запиту: авторський супровід або повна реалізація з майстрами й відповідальністю за результат. Допомагаємо зберегти цілісність задуму на всіх етапах.',items:['Супровід будівельних та оздоблювальних робіт','Комплектація й добір матеріалів та механізмів','Координація майстрів у погодженому форматі','Особисто у Львові; виїзди, дистанційна робота й перевірені партнери в інших регіонах']},
      {title:'Автоматизація',sub:'Технології, що підлаштовуються під вас',body:'Від простого керування світлом до комплексних сценаріїв для квартири, будинку або виробничого приміщення. Добираємо рішення під об’єкт і ваш спосіб користування простором.',items:['Освітлення та мультимедіа: прохідні зони, імітація присутності, світлові сценарії, біодинамічне світло, мультирум','Клімат: опалення, позонний контроль, кондиціонування, вентиляція, зволоження та тепла підлога','Безпека: протікання води, газ і дим, контур безпеки, розумні замки та периметр','Приводи: штори, ролети, ворота, замки, вікна та кватирки','Енергія й побут: «вимкнути все», оптимізація тарифів, автополив і робот-пилосос']}
    ],
    detailNote:'Склад робіт і формат співпраці обговоримо під час дзвінка.',
    approachLabel:'Відповідальність за задум',approachTitle:'Ваша ідея.<br>Наша увага<br>до кожного кроку.',
    approachText:'Ми поруч у ключових рішеннях — від першої розмови до реалізації. Обирайте повний супровід або участь студії на потрібних вам етапах.',steps:['Знайомимося з вами та вашим задумом','Створюємо узгоджену концепцію','Супроводжуємо її втілення'],
    contactLabel:'Почнімо з розмови',geography:'Львів · Україна · Європа',contactTitle:'Яким ви бачите<br>свій простір?',contactText:'Розкажіть про вашу ідею.<br>Разом визначимо наступний крок.',email:'Написати на email ↗',copyright:'© 2026 PISKOR Architect',version:'Дизайн-концепт 02 · на обговорення',houseAlt:'Концептуальний будинок із темного каменю та скла серед сосен',interiorAlt:'Концептуальний інтер’єр з природним каменем, деревом та видом на ліс'
  },
  en: {
    skip:'Skip to content',nav:['Approach','Expertise','Contact'],menu:'Menu',close:'Close',
    place:'Architecture & design · Lviv, Ukraine',hero:'Space.<br>Considered<br><span>in every sense.</span>',heroText:'From the first line to the light that welcomes you home.',cta:'Discuss your vision',scroll:'Discover our approach',concept:'Concept visualization',conceptSub:'Created to explore the website design',
    introLabel:'A complete perspective',intro:'Architecture, interiors, technology.<br><span>One idea. In every detail.</span>',introText:'We bring space, materials and engineering together. From vision to completion, we consider how you will live and work in the spaces we design.',years:'years of experience',projects:'projects',storyLabel:'One space · from idea to everyday life',skipStory:'Explore expertise ↗',sceneNav:['Idea','Architecture','Interior','Realization','Automation'],
    scenes:[['It begins with<br>the way you live.','Your habits, your rhythm, your senses. We find the idea that will bring your future space together.'],['An idea<br>takes shape.','Proportion, natural light and a connection to place. We create architecture with character.'],['Beauty that works.<br>Every day.','Interiors and engineering designed together. Comfort begins with carefully considered decisions.'],['A vision you<br>can feel.','Stone, timber, texture. We select materials and guide the realization of every detail.'],['A home in tune<br>with your rhythm.','Light, climate and security. We bring technology together in scenarios that feel natural to your life.']],
    storyNote:'Scroll or choose a stage',materials:['Stone','Timber','Textile'],system:'“At home” scenario',systemRows:[['Lighting','Warm light'],['Climate','22 °C'],['Curtains','Open']],serviceLabel:'Our expertise',servicesTitle:'From the first idea.<br>To the final detail.',
    services:[
      {title:'Architecture',sub:'Architectural concepts and design',body:'We find a balance between the character of a place, the way you live and the potential of the site. We work with residential, commercial and industrial spaces.',items:['Concept, form and spatial planning','Architectural design and engineering coordination','Façade materials and a connection to the surroundings','Design of projects up to consequence class CC3 in Ukraine']},
      {title:'Interiors & engineering',sub:'Aesthetics, ergonomics and invisible comfort',body:'We design interiors as a complete system, from planning and materials to lighting, climate and engineering. Each decision supports everyday comfort.',items:['Spatial planning, visual direction and visualization','Lighting, electrical and engineering systems','Materials, furniture, mechanisms and equipment','Coordination of aesthetic and technical decisions']},
      {title:'Realization & supervision',sub:'From material selection to completion',body:'Our involvement follows your needs: design supervision or full realization with tradespeople and responsibility for the result. We help preserve the integrity of the original vision at each stage.',items:['Construction and finishing supervision','Specification and selection of materials and mechanisms','Coordination of tradespeople within the agreed scope','On site in Lviv; visits, remote work and trusted partners elsewhere']},
      {title:'Automation',sub:'Technology that adapts to you',body:'From simple lighting control to integrated scenarios for apartments, houses and industrial spaces. We select solutions for your space and the way you use it.',items:['Lighting & multimedia: circulation areas, presence simulation, light scenes, biodynamic lighting and multiroom audio','Climate: heating, zone control, air conditioning, ventilation, humidification and underfloor heating','Security: water leaks, gas and smoke, security zones, smart locks and perimeter monitoring','Motorized systems: curtains, blinds, gates, locks, windows and vents','Energy & home: all-off, tariff optimization, lawn irrigation and robot vacuums']}
    ],detailNote:'We will discuss the scope and format of our work during your call.',approachLabel:'Care for the original vision',approachTitle:'Your vision.<br>Our attention.<br>At every step.',approachText:'We are there for the decisions that matter, from the first conversation to completion. Choose full guidance or our involvement at the stages you need.',steps:['We get to know you and your vision','We develop a coherent concept','We guide its realization'],contactLabel:'Start with a conversation',geography:'Lviv · Ukraine · Europe',contactTitle:'How do you imagine<br>your space?',contactText:'Tell us about your idea.<br>We will find the next step together.',email:'Write us an email ↗',copyright:'© 2026 PISKOR Architect',version:'Design concept 02 · for discussion',houseAlt:'Concept house in dark stone and glass among pine trees',interiorAlt:'Concept interior with natural stone, timber and forest views'
  }
};
let locale = new URLSearchParams(location.search).get('lang') === 'en' ? 'en' : 'uk';
let activeFrame=-1, activeChapter=-1, scrollTick=false, dialogOpener, imageTicket=0, manualUntil=0;
const reduced=matchMedia('(prefers-reduced-motion: reduce)');
const clamp=(n,a=0,b=1)=>Math.min(b,Math.max(a,n));
const L=values=>values[locale==='uk'?0:1];
const assets={climate:'assets/automation/climate.webp',entry:'assets/automation/entry.webp',entryOpen:'assets/automation/entry-open.webp',privacy:'assets/v2/privacy.webp',house:'assets/house-concept.webp',interior:'assets/interior-concept.webp',model:'assets/v2/model.webp',facade:'assets/v2/facade.webp',kitchen:'assets/v2/kitchen.webp',materials:'assets/v2/materials.webp',night:'assets/v2/night.webp'};
const frames=chapters.flatMap((chapter,c)=>chapter.beats.map((beat,b)=>({...beat,chapter:c,beat:b})));
let automation={preset:0,asset:"interior",light:75,temp:22,curtains:false,armed:false};
const presets=[{asset:"interior",light:75,temp:22,curtains:false,armed:false},{asset:"night",light:40,temp:23,curtains:false,armed:false},{asset:"night",light:12,temp:22,curtains:true,armed:false},{asset:"night",light:0,temp:18,curtains:true,armed:true}];
const logo='<img src="assets/piskor-logo.png" width="924" height="234" alt="PISKOR Architect">';
const arrow='<span aria-hidden="true">↗</span>';
function render() {
  const t=copy[locale];
  document.documentElement.lang=locale;
  document.title=locale==='uk'?'PISKOR Architect — Концепт 02':'PISKOR Architect — Concept 02';
  document.body.classList.toggle('reduced',reduced.matches);
  document.querySelector('#app').innerHTML=`
  <a class="skip" href="#main">${t.skip}</a>
  <header class="header">
    <a class="logo" href="#" aria-label="PISKOR Architect">${logo}</a>
    <nav class="nav" aria-label="${t.menu}"><a href="#approach">${t.nav[0]}</a><a href="#story">${t.nav[1]}</a><a href="#contact">${t.nav[2]}</a></nav>
    <div class="header-right"><div class="language" aria-label="Language"><button data-lang="uk" aria-pressed="${locale==='uk'}" aria-label="Українська">UA</button><span>/</span><button data-lang="en" aria-pressed="${locale==='en'}" aria-label="English">EN</button></div><a class="header-call" href="tel:+380974781005">+380 97 478 10 05 ${arrow}</a><button class="menu-button" aria-label="${t.menu}" aria-expanded="false" aria-controls="mobile-menu"><i></i><i></i></button></div>
  </header>
  <nav class="mobile-menu" id="mobile-menu" inert aria-label="${t.menu}"><a href="#approach">${t.nav[0]}</a><a href="#story">${t.nav[1]}</a><a href="#contact">${t.nav[2]}</a><a href="tel:+380974781005">+380 97 478 10 05 ↗</a></nav>
  <main id="main">
    <section class="hero" aria-labelledby="hero-title">
      <div class="hero-picture"><img src="assets/house-concept.webp" width="1536" height="1024" alt="${t.houseAlt}" fetchpriority="high"></div>
      <div class="hero-copy"><div class="eyebrow">${t.place}</div><h1 id="hero-title">${t.hero}</h1><p>${t.heroText}</p><a class="text-link" href="tel:+380974781005">${t.cta}${arrow}</a></div>
      <div class="hero-bottom"><a href="#story" class="scroll"><span aria-hidden="true">↓</span>${t.scroll}</a><div class="hero-caption"><b>${t.concept}</b>${t.conceptSub}</div></div>
    </section>
    <section class="story-intro"><div class="eyebrow">${t.introLabel}</div><h2>${storyUI[locale].title}<br><span>${storyUI[locale].subtitle}</span></h2><p>${storyUI[locale].intro}</p><a class="text-link" href="#story">${storyUI[locale].explore}<span aria-hidden="true">↓</span></a></section>
    <section class="story" id="story" aria-label="${t.storyLabel}">
      <div class="story-sticky">
        <div class="story-top"><a href="#story" class="story-brand">${storyUI[locale].title} <span>${storyUI[locale].subtitle}</span></a><a href="tel:+380974781005" class="story-call">${t.cta} ↗</a></div>
        <div class="stage-visual" aria-hidden="true"><img class="visual-image current" alt="" src="assets/v2/model.webp"><img class="visual-image incoming" alt=""><div class="visual-light"></div><img class="privacy-image" src="assets/v2/privacy.webp" alt=""></div>
        <div class="technical-layer" aria-hidden="true"></div><div class="cinematic-shade"></div>
        <div class="concept-label">${storyUI[locale].concept}</div>
        <div class="beat-nav" role="group" aria-label="${storyUI[locale].points}"></div>
        <div class="hotspots" role="group" aria-label="${storyUI[locale].points}"></div>
        <div class="hotspot-note" hidden></div>
        <div class="chapter-copy"><div class="eyebrow chapter-label"></div><h2></h2><p></p><div class="chapter-actions"><button class="stage-details">${storyUI[locale].detail} <span aria-hidden="true">+</span></button><a href="tel:+380974781005">${storyUI[locale].call} ↗</a></div></div>
        <button class="mobile-automation-trigger" hidden>${locale==='uk'?'Керувати сценаріями':'Control scenarios'} <span aria-hidden="true">↗</span></button>
        <div class="automation-panel" hidden></div>
        <dialog class="mobile-control-sheet" aria-label="${locale==='uk'?'Керування простором':'Space controls'}"><button class="sheet-close" aria-label="${t.close}">×</button><div class="sheet-content"></div></dialog>
        <div class="story-bottom"><div class="story-hint">${storyUI[locale].hint}</div><div class="story-controls"><button class="previous-beat" aria-label="${storyUI[locale].previous}">←</button><div class="chapter-dock" role="group" aria-label="${t.storyLabel}">${chapters.map((c,i)=>`<button data-chapter="${i}" aria-pressed="false">${c.name[locale==='uk'?0:1]}</button>`).join('')}</div><button class="next-beat" aria-label="${storyUI[locale].next}">→</button></div><div class="narrative-progress" aria-hidden="true"><i></i></div></div>
      </div>
    </section>
    <section class="services" id="services"><div class="section-head"><div class="eyebrow">${t.serviceLabel}</div><h2>${t.servicesTitle}</h2></div>${t.services.map((s,i)=>`<div class="service-row"><div class="service-mark" aria-hidden="true">↗</div><button data-service="${i}" aria-haspopup="dialog" aria-label="${s.title}"><div><h3>${s.title}</h3><p>${s.sub}</p></div><span class="arrow-circle" aria-hidden="true">↗</span></button></div>`).join('')}</section>
    <section class="approach" id="approach"><div class="approach-image"><img src="assets/interior-concept.webp" alt="${t.interiorAlt}" width="1536" height="1024" loading="lazy"><div class="image-caption">${t.concept}</div></div><div class="approach-content"><div class="eyebrow">${t.approachLabel}</div><h2>${t.approachTitle}</h2><p>${t.approachText}</p><div class="approach-steps">${t.steps.map((s,i)=>`<div><span aria-hidden="true">—</span>${s}</div>`).join('')}</div></div></section>
    <section class="contact" id="contact"><div class="contact-top"><div class="eyebrow">${t.contactLabel}</div><div class="eyebrow">${t.geography}</div></div><h2>${t.contactTitle}</h2><div class="contact-main"><a class="contact-phone" href="tel:+380974781005">+380 97 478 10 05 ${arrow}</a><p>${t.contactText}</p></div><div class="contact-other"><a href="https://wa.me/380974781005" target="_blank" rel="noopener">WhatsApp ↗</a><a href="viber://chat?number=%2B380974781005">Viber ↗</a><a href="mailto:info@piskor.com.ua">${t.email}</a></div></section>
  </main>
  <footer class="footer"><div class="footer-top"><a class="logo" href="#">${logo}</a><div class="footer-social"><a href="https://www.instagram.com/piskor.com.ua/" target="_blank" rel="noopener">Instagram ↗</a><a href="https://www.behance.net/piskor_com_ua" target="_blank" rel="noopener">Behance ↗</a><a href="https://www.facebook.com/piskor.com.ua/" target="_blank" rel="noopener">Facebook ↗</a></div></div><div class="footer-bottom"><span>${t.copyright}</span><span>${t.version}</span></div></footer>
  <dialog class="detail" aria-labelledby="detail-title"><button class="detail-close" aria-label="${t.close}">×</button><div class="detail-inner"></div></dialog>`;

  bindControls();
  initStory();
}


function technicalArt(layer) {
  const ground='<path d="M170 690L685 425L1380 580L880 900Z"/><path d="M210 720L740 455M280 755L810 485M350 790L880 515M420 825L950 545M360 595L1050 765M480 535L1180 700"/>';
  const zones='<g class="zone-a"><path d="M640 580L1030 440L1260 540L900 720Z"/></g><g class="zone-b"><path d="M265 565L595 455L715 550L425 680Z"/></g><path class="route" d="M750 810L865 735L830 625L580 565L410 615"/>';
  const roof='<path d="M595 395L1070 270L1300 375L840 545Z"/><path d="M595 395V420L840 570L1300 400V375M840 545V570"/>';
  const networks='<g class="network climate"><path d="M490 350L490 435L840 525L1150 405M840 525V595M620 475V550"/></g><g class="network electric"><path d="M380 610L620 535L900 630L1200 520M620 535V420M900 630V480M1050 565V465"/></g><g class="network water"><path d="M420 720L650 610L950 715L1160 620"/></g><g fill="currentColor"><circle cx="490" cy="350" r="5"/><circle cx="1150" cy="405" r="5"/><circle cx="1200" cy="520" r="5"/><circle cx="950" cy="715" r="5"/></g>';
  const assembly=`<g class="assembly-top">${roof}</g><g class="assembly-middle">${networks}</g><g class="assembly-base">${ground}</g><path stroke-dasharray="3 10" d="M595 230V660M840 330V810M1300 210V590"/>`;
  const sun='<path class="sun-path" d="M170 650Q700 -100 1390 610"/><circle cx="1030" cy="220" r="35"/><path d="M1030 160V130M1030 280V310M970 220H940M1090 220H1120M988 178L968 158M1072 178L1092 158"/><path stroke-dasharray="4 12" d="M1020 255L720 690M1000 250L490 650M1050 255L950 640"/>';
  const junction='<circle cx="950" cy="485" r="105"/><circle cx="950" cy="485" r="130" stroke-dasharray="2 14"/><path d="M950 340V380M950 590V635M800 485H845M1055 485H1100"/>';
  const content={site:ground+sun,zones,volume:roof+ground,networks,assembly,sun,junction}[layer]||'';
  return content?`<svg viewBox="0 0 1536 1024" preserveAspectRatio="xMidYMid slice"><g fill="none" stroke="currentColor" stroke-width="1.5">${content}</g></svg>`:'';
}

const decodedImages=new Map();
let sceneLayoutTimer,lastStoryScrollY=scrollY,nativeTouchUntil=0;
function prepareImage(key){
 if(!decodedImages.has(key)){const image=new Image();image.src=assets[key];decodedImages.set(key,image.decode().catch(()=>{}));}
 return decodedImages.get(key);
}
function showImage(key, animate=true) {
 const stage=document.querySelector('.stage-visual');
 if(stage.dataset.asset===key)return;
 stage.dataset.asset=key;
 const token=++imageTicket,current=stage.querySelector('.current'),incoming=stage.querySelector('.incoming');
 // Finish the previous dissolve before a new explicit navigation request.
 if(stage.classList.contains('image-transition'))current.src=incoming.src;
 incoming.getAnimations().forEach(a=>a.cancel());incoming.style.opacity='0';stage.classList.remove('image-transition');
 const apply=()=>{
  if(token!==imageTicket)return;
  incoming.src=assets[key];incoming.style.objectPosition=current.style.objectPosition;
  incoming.style.transform=innerWidth<=700?'scale('+frames[activeFrame].view[0]+')':'none';
  if(!animate||reduced.matches){current.src=assets[key];incoming.style.opacity='0';return;}
  stage.classList.add('image-transition');
  const a=incoming.animate([{opacity:0},{opacity:1}],{duration:1000,easing:'cubic-bezier(.25,.1,.25,1)',fill:'forwards'});
  a.finished.then(()=>{if(token!==imageTicket)return;current.src=assets[key];a.cancel();incoming.style.opacity='0';stage.classList.remove('image-transition');}).catch(()=>{});
 };
 if(!animate)apply();else prepareImage(key).then(apply);
}

function initStory(){
  activeFrame=-1;activeChapter=-1;
  document.body.classList.toggle('reduced',reduced.matches);
  setFrame(0,false);
  updateScroll();
}

function setFrame(index, animate=true){
  index=clamp(index,0,frames.length-1);
  if(index===activeFrame)return;
  activeFrame=index;
  const f=frames[index], c=chapters[f.chapter], ui=storyUI[locale];
  const stage=document.querySelector('.story-sticky');
  clearTimeout(sceneLayoutTimer);stage.classList.toggle('scene-changing',animate&&!reduced.matches);
  sceneLayoutTimer=setTimeout(()=>{positionHotspots(0);stage.classList.remove('scene-changing');},animate&&!reduced.matches?1050:0);
  stage.dataset.stage=String(f.chapter);stage.dataset.layer=f.layer;stage.dataset.automationBeat=String(f.beat);
  const changedChapter=activeChapter!==f.chapter;activeChapter=f.chapter;
  const caption=document.querySelector('.chapter-copy');
  caption.querySelector('.chapter-label').textContent=L(c.name)+' / '+L(f.label);
  caption.querySelector('h2').textContent=L(f.title);
  caption.querySelector('p').textContent=L(f.text);
  if(animate&&!reduced.matches){caption.getAnimations().forEach(a=>a.cancel());caption.animate([{opacity:0,transform:'translateY(16px)'},{opacity:1,transform:'translateY(0)'}],{duration:600,easing:'cubic-bezier(.22,.61,.36,1)'});}
  document.querySelectorAll('[data-chapter]').forEach((b,i)=>b.setAttribute('aria-pressed',String(i===f.chapter)));
  if(innerWidth<=700){const dock=document.querySelector('.chapter-dock'),selected=dock.querySelector('[aria-pressed=true]');dock.scrollTo({left:selected.offsetLeft-dock.offsetLeft-(dock.clientWidth-selected.offsetWidth)/2,behavior:reduced.matches?'instant':'smooth'});}
  document.querySelector('.beat-nav').innerHTML=c.beats.map((b,i)=>`<button data-beat="${i}" aria-pressed="${i===f.beat}"><i aria-hidden="true"></i>${L(b.label)}</button>`).join('');
  document.querySelector('.technical-layer').innerHTML=technicalArt(f.layer);
  const hs=document.querySelector('.hotspots');
  hs.innerHTML=f.points.map((p,i)=>`<button class="hotspot" data-point="${i}" style="--x:${p[0]}%;--y:${p[1]}%" aria-expanded="false" aria-label="${p[locale==='uk'?2:3]}"><span aria-hidden="true">+</span><b>${p[locale==='uk'?2:3]}</b></button>`).join('');
  document.querySelector('.hotspot-note').hidden=true;
  document.querySelector('.automation-panel').hidden=f.chapter!==4;
  document.querySelector('.mobile-automation-trigger').hidden=f.chapter!==4;
  document.querySelector('.previous-beat').disabled=index===0;
  document.querySelector('.next-beat').disabled=index===frames.length-1;
  if(f.chapter===4){systemState.group=['light','climate','security','home'][f.beat];systemState.focus='';if(changedChapter)renderAutomation();updateAutomation();}
  else{const effects=document.querySelector('.device-effects');if(effects)effects.style.display='none';stage.style.removeProperty('--light');stage.style.removeProperty('--warmth');stage.classList.remove('curtains-closed','away');showImage(f.asset,animate);}
  poseFrame(0);
  requestAnimationFrame(()=>positionHotspots(0));
  document.querySelector('.narrative-progress i').style.width=((index+1)/frames.length*100)+'%';
  for(const neighbor of [frames[index+1],frames[index-1]])if(neighbor)prepareImage(neighbor.chapter===4?['interior','climate','entry','house'][neighbor.beat]:neighbor.asset);
}

function poseFrame(){
 if(activeFrame<0)return;
 const f=frames[activeFrame],stage=document.querySelector('.stage-visual');
 positionHotspots(0,f.view[0]);
 stage.style.setProperty('--zoom',f.view[0]);
 stage.style.setProperty('--position',`${f.view[1]}% ${f.view[2]}%`);
 document.querySelector('.technical-layer').style.setProperty('--phase',0);
 document.querySelector('.technical-layer').style.opacity='.75';
}

function jumpFrame(index){
  nativeTouchUntil=0;
  index=clamp(index,0,frames.length-1);
  manualUntil=performance.now()+1000;
  setFrame(index);
  if(!reduced.matches){
    const story=document.querySelector('.story');
    const travel=story.offsetHeight-document.querySelector('.story-sticky').offsetHeight;
    window.scrollTo({top:scrollY+story.getBoundingClientRect().top+(index+.05)/frames.length*travel,behavior:'instant'});
  }
}

function updateScroll(){
  if(reduced.matches)return;
  const story=document.querySelector('.story');if(!story)return;
  const travel=Math.max(1,story.offsetHeight-document.querySelector('.story-sticky').offsetHeight);
  const y=scrollY,top=y+story.getBoundingClientRect().top,bottom=top+travel,previous=lastStoryScrollY;
  lastStoryScrollY=y;
  // Catch native touch inertia that enters after the finger has already lifted.
  if(performance.now()<nativeTouchUntil){
    if(previous>bottom&&y<=bottom){jumpFrame(frames.length-1);return;}
    if(previous<top&&y>=top){jumpFrame(0);return;}
  }
  const position=clamp(-story.getBoundingClientRect().top/travel)*frames.length;
  setFrame(Math.min(frames.length-1,Math.floor(position)),true);
}

function renderAutomation(){
  const ui=storyUI[locale];
  document.querySelector('.automation-panel').innerHTML=automationPanelHTML(ui);
}

function updateAutomation(){
  if(activeChapter!==4)return;
  const panel=document.querySelector('.automation-panel'),ui=storyUI[locale],stage=document.querySelector('.story-sticky');
  panel.querySelector('.preset-name').textContent=automation.preset<0?ui.custom:ui.presets[automation.preset];
  panel.querySelectorAll('[data-preset]').forEach((b,i)=>b.setAttribute('aria-pressed',String(i===automation.preset)));
  panel.querySelector('#light-control').value=automation.light;
  panel.querySelector('#climate-control').value=automation.temp;
  panel.querySelector('#light-output').textContent=automation.light+'%';
  panel.querySelector('#climate-output').textContent=automation.temp+' °C';
  const curtain=panel.querySelector('[data-curtains]');curtain.textContent=automation.curtains?ui.closed:ui.opened;curtain.setAttribute('aria-pressed',String(automation.curtains));
  panel.querySelector('.security-state').textContent=ui.security+' · '+(automation.armed?ui.armed:ui.home);
  stage.style.setProperty('--light',String(.32+automation.light*.0068));
  stage.style.setProperty('--warmth',String((automation.temp-16)/12*.18));
  stage.classList.remove('curtains-closed');stage.classList.toggle('away',automation.armed);
  showImage(automationSceneAsset());
  updateSystems();
}

function toggleMenu(open){
  document.querySelector('.mobile-menu').classList.toggle('open',open);
  document.querySelector('.mobile-menu').inert=!open;
  document.querySelector('.header').classList.toggle('menu-open',open);
  const b=document.querySelector('.menu-button');b.setAttribute('aria-expanded',String(open));b.setAttribute('aria-label',open?copy[locale].close:copy[locale].menu);
  document.body.classList.toggle('locked',open);
  document.querySelector('main').inert=open;document.querySelector('footer').inert=open;
}

function showDetail(chapter,opener){
  const c=chapters[chapter],ui=storyUI[locale],dialog=document.querySelector('.detail');
  dialogOpener=opener;
  dialog.querySelector('.detail-inner').innerHTML=`<div class="eyebrow">${ui.process}</div><h2 id="detail-title">${L(c.name)}</h2><p>${L(c.intro)}</p><div class="stage-deep-dive">${c.beats.map(b=>`<article><h3>${L(b.label)}</h3><p>${L(b.text)}</p><div class="stage-result"><span>${ui.result}</span><p>${L(b.result)}</p></div></article>`).join('')}</div>${chapter>0?`<div class="service-scope"><h3>${copy[locale].serviceLabel}</h3><ul>${copy[locale].services[chapter-1].items.map(item=>`<li>${item}</li>`).join('')}</ul></div>`:''}<a class="text-link" href="tel:+380974781005">${ui.call}${arrow}</a><p class="detail-note">${copy[locale].detailNote}</p>`;
  dialog.showModal();document.body.classList.add('locked');dialog.scrollTop=0;
}

function bindControls(){
  document.querySelectorAll('[data-lang]').forEach(b=>b.addEventListener('click',()=>{
    if(locale===b.dataset.lang)return;
    const y=scrollY,frame=activeFrame,inStory=document.querySelector('.story').getBoundingClientRect().top<=0&&document.querySelector('.story').getBoundingClientRect().bottom>innerHeight;
    locale=b.dataset.lang;const url=new URL(location.href);url.searchParams.set('lang',locale);history.replaceState(null,'',url);
    document.body.classList.remove('locked');render();window.scrollTo({top:y,behavior:'instant'});if(inStory)jumpFrame(frame);
    document.querySelector(`[data-lang="${locale}"]`).focus({preventScroll:true});
  }));
  document.querySelector('.menu-button').addEventListener('click',()=>toggleMenu(!document.querySelector('.mobile-menu').classList.contains('open')));
  // Anchor navigation skips the intervening narrative; manual scrolling still plays it.
  document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{
    e.preventDefault();toggleMenu(false);const hash=a.getAttribute('href');
    if(hash==='#story'){jumpFrame(0);if(reduced.matches)document.querySelector('#story').scrollIntoView({behavior:'instant'});}
    else{const target=hash==='#'?document.body:document.querySelector(hash);if(target)window.scrollTo({top:hash==='#'?0:scrollY+target.getBoundingClientRect().top,behavior:'instant'});}
    history.replaceState(null,'',location.pathname+location.search+hash);
  }));
  document.querySelector('.story').addEventListener('click',e=>{
    const b=e.target.closest('button');if(!b)return;
    systemClick(b);
    if(b.dataset.chapter!==undefined)jumpFrame(Number(b.dataset.chapter)*4);
    if(b.dataset.beat!==undefined){const selected=Number(b.dataset.beat);jumpFrame(activeChapter*4+selected);document.querySelectorAll('[data-beat]')[selected].focus({preventScroll:true});}
    if(b.classList.contains('next-beat'))jumpFrame(activeFrame+1);
    if(b.classList.contains('previous-beat'))jumpFrame(activeFrame-1);
    if(b.classList.contains('stage-details'))showDetail(activeChapter,b);
    if(b.classList.contains('mobile-automation-trigger')){const sheet=document.querySelector('.mobile-control-sheet');sheet.querySelector('.sheet-content').append(document.querySelector('.automation-panel'));sheet.showModal();document.body.classList.add('locked');}
    if(b.classList.contains('sheet-close'))document.querySelector('.mobile-control-sheet').close();
    if(b.dataset.point!==undefined){
      const panel=document.querySelector('.hotspot-note'),point=frames[activeFrame].points[Number(b.dataset.point)],open=b.getAttribute('aria-expanded')!=='true';
      document.querySelectorAll('[data-point]').forEach(p=>p.setAttribute('aria-expanded','false'));b.setAttribute('aria-expanded',String(open));
      panel.hidden=!open;panel.innerHTML=`<strong>${point[locale==='uk'?2:3]}</strong><p>${L(frames[activeFrame].text)}</p>`;
    }
    if(b.dataset.preset!==undefined){const i=Number(b.dataset.preset);automation={preset:i,...presets[i]};updateAutomation();}
    if(b.hasAttribute('data-curtains')){systemState.focus='curtains';automation.curtains=!automation.curtains;automation.preset=-1;updateAutomation();}
    if(b.hasAttribute('data-reset')){automation={preset:0,...presets[0]};updateAutomation();}
  });
  document.querySelector('.automation-panel').addEventListener('input',e=>{if(e.target.dataset.control){automation[e.target.dataset.control]=Number(e.target.value);automation.preset=-1;updateAutomation();}if(e.target.dataset.systemRange){systemState[e.target.dataset.systemRange]=Number(e.target.value);automation.preset=-1;updateAutomation();}});
  document.querySelectorAll('[data-service]').forEach(b=>b.addEventListener('click',()=>showDetail(Number(b.dataset.service)+1,b)));
  const sheet=document.querySelector('.mobile-control-sheet');
  sheet.addEventListener('close',()=>{document.querySelector('.story-sticky').append(document.querySelector('.automation-panel'));document.body.classList.remove('locked');document.querySelector('.mobile-automation-trigger').focus({preventScroll:true});});
  sheet.addEventListener('click',e=>{if(e.target===sheet){const r=sheet.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)sheet.close();}});
  const dialog=document.querySelector('.detail');
  dialog.querySelector('.detail-close').addEventListener('click',()=>dialog.close());
  dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close();}});
  dialog.addEventListener('close',()=>{document.body.classList.remove('locked');dialogOpener?.focus({preventScroll:true});});
}

addEventListener('scroll',()=>{if(!scrollTick){scrollTick=true;requestAnimationFrame(()=>{updateScroll();scrollTick=false;});}},{passive:true});
addEventListener('resize',()=>{if(innerWidth>700){const sheet=document.querySelector('.mobile-control-sheet');if(sheet.open)sheet.close();if(!document.querySelector('dialog[open]'))toggleMenu(false);}updateScroll();});
addEventListener('keydown',e=>{if(e.key==='Escape'){document.querySelector('.hotspot-note').hidden=true;document.querySelectorAll('[data-point]').forEach(b=>b.setAttribute('aria-expanded','false'));if(document.querySelector('.mobile-menu.open')){toggleMenu(false);document.querySelector('.menu-button').focus();}}});
reduced.addEventListener('change',()=>{const index=activeFrame;document.querySelectorAll('.story *').forEach(e=>e.getAnimations().forEach(a=>a.cancel()));document.body.classList.toggle('reduced',reduced.matches);activeFrame=-1;setFrame(index,false);if(!reduced.matches)jumpFrame(index);});

render();

// Anchors are source-image coordinates, projected through the same fit and zoom as the photo.
function positionHotspots(amount=0,zoom=frames[activeFrame]?.view[0]||1){
 if(activeFrame<0)return;
 const f=frames[activeFrame],root=document.querySelector('.story-sticky'),visual=root.querySelector('.stage-visual'),mobile=innerWidth<=700;
 if(mobile){const top=f.chapter===4?180:136,height=Math.max(40,root.querySelector('.chapter-copy').offsetTop-top-20);visual.style.top=top+'px';visual.style.height=height+'px';visual.style.bottom='auto';}else{visual.style.top='';visual.style.height='';visual.style.bottom='';}
 const technical=root.querySelector('.technical-layer');technical.style.top=mobile?visual.style.top:'';technical.style.height=mobile?visual.style.height:'';
 const w=visual.clientWidth,h=visual.clientHeight,iw=1536,ih=1024,fit=(mobile?Math.min:Math.max)(w/iw,h/ih),rw=iw*fit,rh=ih*fit;
 const px=mobile?.5:f.view[1]/100,py=mobile?.5:f.view[2]/100,top=visual.offsetTop;
 root.querySelectorAll('[data-point]').forEach((b,i)=>{const p=f.points[i],x=((w-rw)*px+rw*p[0]/100-w/2)*zoom+w/2,y=((h-rh)*py+rh*p[1]/100-h/2)*zoom+h/2+top;
 b.style.left=x+'px';b.style.top=y+'px';b.hidden=x<22||x>w-22||y<top+18||y>top+h-18||(!mobile&&y>root.querySelector('.chapter-copy').offsetTop&&x<root.querySelector('.chapter-copy').offsetWidth+70)||(!reduced.matches&&amount>.55);
 });
}

// Wheel and touch share the same entry/step behavior; one gesture owns one change.
let wheelLast=0,wheelLatched=false,wheelTotal=0,wheelDirection=0,touchStart=null;
function storyInputTarget(target){return !document.querySelector('dialog[open]')&&!target.closest('input,.automation-panel,.hotspot-note');}
function storyPinned(){const r=document.querySelector('.story').getBoundingClientRect();return r.top<=2&&r.bottom>=innerHeight-2;}
function storyEntry(direction){
 const r=document.querySelector('.story').getBoundingClientRect();
 if(direction<0&&r.bottom>=-2&&r.bottom<innerHeight-2)return frames.length-1;
 if(direction>0&&r.top>2&&r.top<=innerHeight)return 0;
 return null;
}
function stepStory(direction){
 const entry=storyEntry(direction);
 if(entry!==null){jumpFrame(entry);return;}
 const next=activeFrame+direction;
 if(next<0||next>=frames.length){
  const story=document.querySelector('.story'),top=scrollY+story.getBoundingClientRect().top;
  // A native smooth scroll can re-enter and traverse scenes while exiting.
  window.scrollTo({top:direction<0?Math.max(0,top-innerHeight):top+story.offsetHeight,behavior:'instant'});
 }else jumpFrame(next);
}
addEventListener('wheel',e=>{
 if(e.ctrlKey||!e.deltaY||Math.abs(e.deltaX)>Math.abs(e.deltaY)||!storyInputTarget(e.target))return;
 const direction=Math.sign(e.deltaY),now=performance.now();
 if(now-wheelLast>180||direction!==wheelDirection){wheelLatched=false;wheelTotal=0;}
 wheelLast=now;wheelDirection=direction;
 const delta=e.deltaY*(e.deltaMode===1?16:e.deltaMode===2?innerHeight:1);
 const r=document.querySelector('.story').getBoundingClientRect();
 const crossing=direction<0&&r.bottom<0&&r.bottom-delta>=0?frames.length-1:direction>0&&r.top>innerHeight&&r.top-delta<=innerHeight?0:null;
 if(!storyPinned()&&storyEntry(direction)===null&&crossing===null)return;
 e.preventDefault();
 if(wheelLatched)return;
 wheelTotal+=delta;
 if(Math.abs(wheelTotal)>=24){wheelLatched=true;if(crossing!==null)jumpFrame(crossing);else stepStory(direction);}
},{passive:false});
addEventListener('touchstart',e=>{
 touchStart=e.touches.length===1&&storyInputTarget(e.target)?{x:e.touches[0].clientX,y:e.touches[0].clientY,done:false}:null;
},{passive:true});
addEventListener('touchmove',e=>{
 if(!touchStart||e.touches.length!==1)return;
 const dx=e.touches[0].clientX-touchStart.x,dy=touchStart.y-e.touches[0].clientY;
 if(Math.abs(dx)>Math.abs(dy)&&!touchStart.done)return;
 nativeTouchUntil=performance.now()+2000;
 const direction=Math.sign(dy);
 if(!touchStart.done&&!storyPinned()&&storyEntry(direction)===null)return;
 e.preventDefault();
 if(!touchStart.done&&Math.abs(dy)>35){touchStart.done=true;stepStory(direction);}
},{passive:false});
addEventListener('touchend',()=>touchStart=null,{passive:true});
addEventListener('touchcancel',()=>touchStart=null,{passive:true});
addEventListener('keydown',e=>{
 if(e.altKey||e.ctrlKey||e.metaKey||e.target.closest('button,a,input,textarea,select')||!storyInputTarget(e.target))return;
 let direction=['ArrowDown','PageDown',' '].includes(e.key)?1:['ArrowUp','PageUp'].includes(e.key)?-1:0;
 if(e.shiftKey&&e.key===' ')direction=-1;
 if(!direction||!storyPinned()&&storyEntry(direction)===null)return;
 e.preventDefault();if(!e.repeat)stepStory(direction);
});
